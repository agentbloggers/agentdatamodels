global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    repo: agentdatamodels

scrape_configs:
  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:8889"]
