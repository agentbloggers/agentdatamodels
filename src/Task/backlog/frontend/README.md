# `src/Task/backlog/frontend/` — frontend backlog

Tasks that would live under a future `src/frontend/` root (if we ever
create one). Today the repo deliberately avoids a bundled frontend —
see each item's **why** for the tradeoff we deferred.
