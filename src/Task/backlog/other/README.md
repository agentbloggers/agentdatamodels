# `src/Task/backlog/other/` — unclassified backlog

Things that don't fit under `frontend/` or `voice-chat/`. If this
folder grows more than a handful of items, promote the common
theme to its own subdirectory (e.g. `dashboard/`, `routines/`,
`agent-sdk/`).
