# `src/Task/backlog/voice-chat/` — voice-chat backlog

Improvements to `src/live/voice-chat/`. Each item is scoped tight
enough to land in a single PR without expanding `eval.mjs` by more
than ~5 new checks.
